# 📅 주말 근무시간 기능 가이드

## 🎯 개요

직원별로 주말(토·일) 근무시간을 별도로 설정할 수 있는 기능입니다.
평일과 주말의 근무시간이 다른 경우에만 입력하면 되며, **기존 직원 데이터에 영향을 주지 않습니다**.

---

## 📋 주요 특징

### ✅ 선택적 입력
- 주말 근무시간 필드는 **선택사항**입니다
- 입력하지 않으면 자동으로 평일 근무시간이 적용됩니다
- 기존 직원들은 재설정할 필요가 없습니다

### 🔄 자동 구분
- 시스템이 자동으로 오늘 날짜의 요일을 확인합니다
- 토요일(6) 또는 일요일(0)인 경우 주말 근무시간을 사용합니다
- 주말 근무시간이 없으면 평일 근무시간을 사용합니다

### 🎨 직관적 UI
- 주황색 배경의 별도 섹션으로 표시됩니다
- 평일 근무시간과 명확히 구분됩니다
- 도움말 메시지가 표시됩니다

---

## 🗄️ 데이터베이스 마이그레이션

### SQL 실행 방법

1. **Supabase 대시보드 접속**
   - https://supabase.com/dashboard 로그인
   - 프로젝트 선택: `facility-management-hub`

2. **SQL Editor 열기**
   - 좌측 메뉴에서 `SQL Editor` 클릭
   - "New query" 버튼 클릭

3. **마이그레이션 SQL 복사 및 실행**
   ```sql
   -- 주말 근무시간 컬럼 추가
   ALTER TABLE employees 
   ADD COLUMN IF NOT EXISTS weekend_start_time TIME,
   ADD COLUMN IF NOT EXISTS weekend_end_time TIME;

   -- 컬럼 설명 추가
   COMMENT ON COLUMN employees.weekend_start_time IS '주말(토·일) 근무 시작 시간 (선택사항, NULL이면 평일 근무시간 사용)';
   COMMENT ON COLUMN employees.weekend_end_time IS '주말(토·일) 근무 종료 시간 (선택사항, NULL이면 평일 근무시간 사용)';
   ```

4. **실행 확인**
   - "Run" 버튼 클릭
   - 성공 메시지 확인
   - 또는 파일로 제공된 `add-weekend-hours-migration.sql` 사용

---

## 👤 사용자 가이드

### 직원 추가/수정 화면

1. **"직원 추가" 또는 "수정" 버튼 클릭**

2. **기본 정보 입력**
   - 이름 (필수)
   - 직책
   - 연락처
   - 이메일
   - 입사일

3. **주말 근무시간 설정 (선택)**
   - 주황색 박스로 표시된 "주말 근무시간 (토·일)" 섹션
   - **시작 시간**: 예) 10:00
   - **종료 시간**: 예) 16:00
   - **비워두면**: 평일 근무시간이 자동 적용됨

4. **저장**
   - "등록" 또는 "수정" 버튼 클릭

### 직원 상세보기

- 주말 근무시간이 설정된 경우:
  - 주황색 박스로 표시됩니다
  - "평일과 다른 주말 근무시간이 설정되어 있습니다" 메시지 표시
- 주말 근무시간이 없는 경우:
  - 표시되지 않음 (평일 근무시간 사용)

---

## 💻 개발자 가이드

### JavaScript API

#### 1. 근무시간 가져오기

```javascript
// 특정 날짜의 근무시간 가져오기
const workTime = unifiedApp.getEmployeeWorkTime(employee, new Date());

// 반환값:
{
    start_time: '09:00',  // 또는 weekend_start_time
    end_time: '18:00',    // 또는 weekend_end_time
    is_weekend: false     // true면 주말 근무시간 사용됨
}
```

#### 2. 근태 체크

```javascript
// 현재 시간 기준으로 근태 체크
const attendance = unifiedApp.checkAttendanceTime(employee);

// 반환값:
{
    work_start: '09:00',
    work_end: '18:00',
    current_time: '09:15',
    is_weekend: false,
    is_late: true,
    status: '지각'  // 또는 '정상'
}
```

### 로직 흐름

```
1. 날짜 체크 (getDay())
   ↓
2. 주말인가? (0=일요일, 6=토요일)
   ↓
3. YES → weekend_start_time이 있는가?
   ↓
4. YES → 주말 근무시간 사용
   NO  → 평일 근무시간 사용
```

---

## 📊 예시 시나리오

### 시나리오 1: 주말 근무시간이 다른 직원

**설정:**
- 평일: 09:00 ~ 18:00
- 주말: 10:00 ~ 16:00 ✅ 입력

**결과:**
- 월~금: 09:00 ~ 18:00 적용
- 토~일: 10:00 ~ 16:00 적용

### 시나리오 2: 평일/주말 동일한 직원

**설정:**
- 평일: 09:00 ~ 18:00
- 주말: (입력 안 함) ❌

**결과:**
- 월~일: 09:00 ~ 18:00 적용

### 시나리오 3: 기존 직원 (마이그레이션 후)

**설정:**
- 평일: 08:00 ~ 17:00 (기존 데이터)
- 주말: NULL (새 컬럼, 입력 안 함)

**결과:**
- 월~일: 08:00 ~ 17:00 적용
- **데이터 재입력 불필요** ✅

---

## 🔧 문제 해결

### Q1: 주말 근무시간을 입력했는데 적용이 안 돼요
**A**: 다음을 확인하세요:
- SQL 마이그레이션이 실행되었는지 확인
- 시작/종료 시간 모두 입력했는지 확인
- 브라우저 캐시 삭제 후 재접속

### Q2: 기존 직원들도 다시 설정해야 하나요?
**A**: **아니요!** 주말 근무시간이 평일과 동일하면 입력할 필요가 없습니다.

### Q3: 주말 근무시간을 삭제하고 싶어요
**A**: 직원 수정 화면에서 시작/종료 시간을 모두 비우고 저장하면 됩니다.

---

## 📝 변경 이력

### 2026-05-30
- ✅ 주말 근무시간 기능 추가
- ✅ `weekend_start_time`, `weekend_end_time` 컬럼 추가
- ✅ 자동 평일/주말 구분 로직 구현
- ✅ UI 개선 (주황색 섹션, 도움말 메시지)
- ✅ `getEmployeeWorkTime()` 헬퍼 함수 추가
- ✅ `checkAttendanceTime()` 근태 체크 함수 추가

---

## 🎉 완료!

이제 주말 근무시간이 평일과 다른 직원들을 편리하게 관리할 수 있습니다!

**다음 단계:**
1. SQL 마이그레이션 실행
2. 필요한 직원들의 주말 근무시간 입력
3. 근태관리 시스템에서 자동으로 적용됨

---

**문의:** 추가 기능이 필요하면 언제든지 요청하세요! 🚀
